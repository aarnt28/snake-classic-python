services:
  snakeclassic:
    build: .
    container_name: snakeclassic
    environment:
      - DISPLAY=${DISPLAY}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
      - snake-data:/root/.snake_classic
    stdin_open: true
    tty: true
    command: ["snake-classic"]

volumes:
  snake-data:
